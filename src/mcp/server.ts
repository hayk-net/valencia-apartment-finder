/**
 * ── The MCP server lives here — but we build it TOGETHER. ──
 *
 * This is the learning milestone of the project: a Model Context Protocol
 * server that exposes your apartment database to Claude (search_listings,
 * get_listing, run_saved_search, refresh_source…), so you can literally ask
 * Claude "any new 3-beds under 330K this week?" against your own data.
 *
 * We'll write it step by step with explanations when you're ready.
 * Until then this file is just a friendly placeholder.
 */
console.log('Not built yet — this is the milestone we do together, step by step.');
console.log('Meanwhile: `npm run dev` starts the app at http://localhost:5173');
